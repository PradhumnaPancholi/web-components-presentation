import {Heading, Text} from 'spectacle'
import React from 'react'

export const Intro = () => {
    return(
        <div>
            <Heading>Web Components</Heading>
            <Text>A set of web platform API's that allow us to create custom, reusable, and encapsulated html tags to use in web pages/web apps.</Text>
        </div>
    )
}
